---
title: "🦆 Big year 2026 🐦‍⬛"
format:
  html:
    css: styles.css
execute:
  echo: false
  warning: false
  message: false
  runtime: shiny
project:
    execute-dir: project
---

```{r}
scripts <- list.files("scripts/", pattern = "\\.R$", full.names = TRUE)
invisible(lapply(scripts, source))

obs <- make_obs_table(
  users = "splendidfairywren,kjaeck", # Don't add commas between names!
  year = 2026,
  taxon_id = 3,
  only_species = TRUE
)
```
# Summary
::: {.stat-grid}
```{r results="asis"}
cat(make_author_stat_cards(obs))
```
:::

## Rote Liste (Rote-Liste-Zentrum)
::: {.stat-grid}
```{r results="asis"}
cat(make_rlz_stat_cards(obs))
```
:::

# Accumulative Species Plot
```{r}
plotly::ggplotly(plot_accum_species(obs))
```

# Interactive Observation Map
```{r}
plot_obs_map(obs)
```

# Individual Observations Table
```{r}
make_obs_html_table(obs)
```

# Observed Species Table
```{r}
make_species_html_table(obs)
```